# Track {{X}} — {{TRACK TITLE}}

**Owner:** Track {{X}} agent (single writer — no other agent edits this file).
**Master plan:** `{{MASTER_PLAN_PATH}}` (goal, shared resources, workflow
rules — read it and the CLAUDE.md workflow section first).
**Dashboard line owned in master:** the `Track {{X}}:` line.

## Approved decisions ({{USER}}, dated)

<!-- Dated records of what the user authorized: the decision, the rationale,
the accepted risks, and any consequences other tracks must know (also mirror
those into the master's Shared resources section via a proposal). -->

*(none yet)*

## Checklist

<!-- Rules of thumb, earned the hard way:
- Every task gets a pass/fail criterion or a number to hit.
- Gates that pick between options carry an explicit decision rule
  ("pick the smallest N with mem ≤ 80% and wall ≤ 2.5 d") so the agent can
  decide without a round-trip.
- Validation gates come BEFORE expensive commitments (prove the pipeline on
  a small run before the production run).
- Benchmarks measured on other hardware are UNVALIDATED here until
  re-measured — say so in the task.
-->

- [ ] {{task}}
  - [ ] {{subtask with pass/fail criterion}}

## Proposed adjustments (pending {{USER}}'s approval — do not execute)

*(none yet)*

## Reports (append-only; one short dated block per completed task)

<!-- Template:
### YYYY-MM-DD — <task name>
- What: <one line>
- Result: <key numbers / pass-fail>
- Artifacts: <paths — run outputs, experiment dirs, figures>
- Notes: <surprises, decisions taken, anything the supervisor/user must know>
-->
